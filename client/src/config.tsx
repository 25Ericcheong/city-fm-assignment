const BACKEND_URL = "http://localhost:8080/api";

export const routes = {
  GET_PRODUCTS: `${BACKEND_URL}/products`,
  GET_FX_RATES: `${BACKEND_URL}/fx-rates`,
  POST_ORDERS: `${BACKEND_URL}/orders`,
};
